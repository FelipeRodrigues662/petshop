import { Component } from '@angular/core';

const newLocal = '<app-navbar></app-navbar><router-outlet></router-outlet>';

@Component({
  selector: 'app-frame-page',
  template: newLocal,
})
export class FramePageComponent {
  
}
